#include "main.h"

/**
 * _isalpha - check if a character is alphabetic
 * @c: The Character to be checked
 * Return: 1 if character is letter, lower or upper case,0  otherwise
 */
int _isalpha(int c)
{
if ((c >= 'a' && c <= 'a' || (c >= 'A' && c <= 'Z'))
{
return (1);
}
else
{
return (0);
}

}
